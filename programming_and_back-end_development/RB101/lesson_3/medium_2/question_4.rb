puts "My string looks like this now: #{my_string}" # pumpkinsrutabaga
puts "My array looks like this now: #{my_array}" # ["pumpkins"]

#  As in the previous exerscise, a_string_param << 'rutabaga' references to the same object, 
#  but an_array_param = ['pumpkins', 'rutabaga'] creates a new object, so the original array doesn't mutate.
